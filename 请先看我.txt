本次更新，支持了保存上一次的视频，
设置双屏(占用gpu)
开机自启动

以上功能均需要先执行install.sh(安装)(在命令行执行sudo sh install.sh) [需要管理员权限]
正常启动:执行start(normal)
最小化启动:执行start
默认没有再提供壁纸了，通过设置，可以设置壁纸


根据文管的dde-desktop加上透明，加deepin-dreamscene加上gui来做的操作

目前阶段(将就能用)